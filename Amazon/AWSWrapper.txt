#!/bin/bash 

dns=$(aws ec2 describe-instances --query 'Reservations[*].Instances[*].PublicDnsName' --output text)

echo $dns

for i in $dns; do
	echo DNS: $i
	./init 7 | ssh -t -i "C:/Users/Ben/Dropbox/Amazon/ec2.pem" ubuntu@$i 'pas=puffin;echo Password is $pas'

	# Need to confirm host?
	echo "Yes"
done

